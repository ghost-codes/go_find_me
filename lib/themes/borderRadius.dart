import 'package:flutter/rendering.dart';

class ThemeBorderRadius {
  static BorderRadius bigRadiusAll = BorderRadius.circular(30);
  static BorderRadius smallRadiusAll = BorderRadius.circular(10);
}
